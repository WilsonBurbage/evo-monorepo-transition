export enum FormSchemaItemType {
  button = 'button',
  checkbox = 'checkbox',
  currency = 'currency',
  date = 'date',
  number = 'number',
  select = 'select',
  text = 'text',
  textArea = 'textArea',
  time = 'time',
}
