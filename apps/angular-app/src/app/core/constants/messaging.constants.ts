export const DEFAULT_DELETION_CONFIRMATION_MESSAGE =
  'Are you sure you wish to delete this item?';

export const DEFAULT_DELETION_CONFIRMATION_MESSAGE_WITH_JOINS_WARNING = `${DEFAULT_DELETION_CONFIRMATION_MESSAGE} - any items associated with it will also be deleted.`;

export const DEFAULT_VALIDATION_MESSAGES: { [key: string]: string } = {
  required: 'This field is required',
};

export const DEFAULT_ASYNC_ERROR_MESSAGE = `Sorry, that didn't work. Please check your internet connection and try again.`;
